# tokenizer-bench artifacts — never commit the venv, datasets, or model weights.
# (Comments MUST be on their own line — Git does not support inline `#` comments.)
.venv/
__pycache__/
*.pyc
# exported per-arm token streams — includes corpus.txt (the private source corpus)
data/
# training logs / metrics
runs/
# model weights
checkpoints/
*.pt
*.bin
.streamlit/secrets.toml
